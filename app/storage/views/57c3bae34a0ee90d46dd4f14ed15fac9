<div class="topgame">
	<h3>GAME Hay nhất</h3>
	<ul>
		<?php foreach($games as $key => $value): ?>
			<?php $url = CommonGame::getUrlGame($value->slug); ?>
			<li>
				<div class="topgame-image">
					<img src="<?php echo url(UPLOAD_GAME_AVATAR . '/' .  $value->image_url); ?>" alt="<?php echo $value->name; ?>" />
				</div>
				<div class="topgame-text">
					<a href="<?php echo $url; ?>"><?php echo limit_text($value->name, TEXTLENGH); ?></a>
					<span><?php echo getZero($value->count_play); ?> lượt chơi</span>
				</div>
				<div class="clearfix"></div>
			</li>
		<?php endforeach; ?>
	</ul>
</div>